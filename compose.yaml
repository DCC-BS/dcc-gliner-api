services:
  gliner-api:
    build: .
    container_name: dcc-gliner-api
    ports:
      - "8000:8000"
    volumes:
      - hf-cache:/app/.cache/huggingface
    restart: unless-stopped

volumes:
  hf-cache:
